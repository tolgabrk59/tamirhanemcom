import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Değerlendirmeler - Tamirhanem',
  description: 'Gerçek müşteri deneyimlerini ve hizmet değerlendirmelerini keşfedin.',
}

export default function YorumlarLayout({ children }: { children: React.ReactNode }) {
  return children
}
